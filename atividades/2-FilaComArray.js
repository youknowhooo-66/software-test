function FilaComArray() {
    const itens = [];
    itens.push('A');
    itens.push('B');
    itens.push('C');
    console.log(itens.shift());
    console.log(itens);
}
export default FilaComArray;

FilaComArray();